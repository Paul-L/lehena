/dist
.env
.DS_Store
/uploads
/node_modules
yarn-error.log

.idea

coverage

!src/**

./tsconfig.tsbuildinfo
medusa-db.sql
build
.cache

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

.medusa

# Locally-linked plugin sources (cf. ADR-001). Plugin code lives in its
# own repo; here we only commit the package.json file: reference + yalc.lock.
.yalc